# AGENTS.md
Do not make changes to it unless explicitly requested by user.

# Python Execution

- Use `uv python` for Python commands when applicable.

# Minimal Workflow That Scales
main
  ├── feature/x
  ├── fix/y
  ├── experiment/z

# Branches

- Clear narrative
- Merged or deleted quickly, not after a thousand commits
- naming: feature/<short-descriptor>, fix/<short-descriptor>, experiment/<short-descriptor>, refactor/<short-descriptor>
